name: HTTP production response A/B

on:
  push:
    branches:
      - experiment/sparse-response-construction
    paths-ignore:
      - handoff.md
      - docs/**

concurrency:
  group: http-production-response-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  benchmark:
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        threaded: [false, true]
    steps:
      - name: Check out experiment
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - name: Set up Perl
        uses: shogo82148/actions-setup-perl@c3e9892cade8a01db1a8498b756f48d6ca98b678 # v1
        with:
          perl-version: '5'
          multi-thread: ${{ matrix.threaded }}

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install --yes libssl-dev

      - name: Install current Linux::Event main and dependencies
        run: |
          cpanm --notest https://github.com/haxmeister/perl-linux-event/archive/1c3de59e395e05e79c735f5d5ef35cd5021e8c55.tar.gz
          cpanm --notest 'Uniform::HTTP::Auth@0.02' || cpanm --notest https://github.com/haxmeister/perl-Uniform-HTTP/archive/b2b243957a8f82dfef6081431d7e5e6f84dc1b08.tar.gz
          cpanm --notest --installdeps .

      - name: Build and test
        run: |
          perl Makefile.PL
          make
          make test

      - name: Build and test exact production baseline
        run: |
          git fetch --depth=1 origin 2245560e8a3dfddd88a4974d52d2168da4cbbbc3
          git worktree add --detach "$RUNNER_TEMP/http-baseline" FETCH_HEAD
          cd "$RUNNER_TEMP/http-baseline"
          perl Makefile.PL
          make
          make test

      - name: Same-run production comparisons
        run: |
          export BENCH_BASE_TREE="$RUNNER_TEMP/http-baseline"
          perl -Mblib bench/run-http-comparison.pl --servers=linuxevent_baseline_content_type,linuxevent_content_type --requests=100000 --warmup=10000 --repeats=7 --response-bytes=32 --json=bench/results/ci-production-get32.json
          perl -Mblib bench/run-http-comparison.pl --servers=linuxevent_baseline_content_type,linuxevent_content_type --requests=50000 --warmup=5000 --repeats=7 --response-bytes=16384 --json=bench/results/ci-production-get16k.json
          perl -Mblib bench/run-http-comparison.pl --servers=linuxevent_baseline_content_type,linuxevent_content_type --requests=50000 --warmup=5000 --repeats=7 --request-body-bytes=4096 --response-bytes=32 --json=bench/results/ci-production-post4k.json

      - name: Production diagnostic smoke
        run: perl -Mblib bench/run-http-transaction-ladder.pl --smoke

      - name: Upload benchmark results
        uses: actions/upload-artifact@v4
        with:
          name: production-response-threaded-${{ matrix.threaded }}
          path: bench/results/ci-production-*.json
